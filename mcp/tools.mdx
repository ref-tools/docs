---
title: 'MCP Tools Reference'
description: 'Complete reference for Ref MCP tools and their usage'
---

## Available Tools

The Ref MCP server provides two powerful tools that AI agents can use to access documentation and web content seamlessly.

<CardGroup cols={2}>
  <Card
    title="ref_search_documentation"
    icon="magnifying-glass"
  >
    Search through indexed documentation with semantic understanding
  </Card>
  <Card
    title="ref_read_url"
    icon="link"
  >
    Extract and read content from any publicly accessible URL
  </Card>
</CardGroup>

## ref_search_documentation

Search through Ref's indexed documentation using natural language queries.

### Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `query` | string | Yes | Search query in natural language |
| `maxResults` | integer | No | Maximum results to return (default: 10, max: 50) |
| `sources` | array | No | Specific documentation sources to search |

### Usage Examples

<AccordionGroup>
  <Accordion title="Basic Search">
    **AI Conversation**:
    ```text
    You: How do I use React hooks?
    
    Claude: I'll search for React hooks documentation.
    
    [Uses ref_search_documentation with query: "React hooks"]
    
    Based on the React documentation, hooks are functions that let you "hook into" React state and lifecycle features from function components...
    ```
    
    **Direct Tool Call**:
    ```json
    {
      "name": "ref_search_documentation",
      "arguments": {
        "query": "React hooks"
      }
    }
    ```
  </Accordion>

  <Accordion title="Specific Framework Search">
    **AI Conversation**:
    ```text
    You: Show me Next.js API routes examples
    
    Claude: Let me search for Next.js API routes documentation.
    
    [Uses ref_search_documentation with query: "Next.js API routes examples"]
    
    Here are some examples of Next.js API routes from the documentation...
    ```
    
    **Direct Tool Call**:
    ```json
    {
      "name": "ref_search_documentation",
      "arguments": {
        "query": "Next.js API routes examples",
        "maxResults": 5
      }
    }
    ```
  </Accordion>

  <Accordion title="Source-Specific Search">
    **AI Conversation**:
    ```text
    You: Find TypeScript interface documentation
    
    Claude: I'll search specifically in TypeScript documentation.
    
    [Uses ref_search_documentation with sources filter]
    
    According to the TypeScript documentation, interfaces are used to define the shape of objects...
    ```
    
    **Direct Tool Call**:
    ```json
    {
      "name": "ref_search_documentation",
      "arguments": {
        "query": "TypeScript interfaces",
        "sources": ["typescript", "microsoft-docs"],
        "maxResults": 8
      }
    }
    ```
  </Accordion>

  <Accordion title="Problem-Solving Search">
    **AI Conversation**:
    ```text
    You: How do I fix CORS errors in Express.js?
    
    Claude: Let me search for Express.js CORS documentation and solutions.
    
    [Uses ref_search_documentation with query: "Express.js CORS errors fix"]
    
    Based on the Express documentation, CORS errors can be resolved by...
    ```
    
    **Direct Tool Call**:
    ```json
    {
      "name": "ref_search_documentation",
      "arguments": {
        "query": "Express.js CORS errors fix middleware"
      }
    }
    ```
  </Accordion>
</AccordionGroup>

### Response Format

```json
{
  "results": [
    {
      "title": "React Hooks - Overview",
      "url": "https://react.dev/reference/react",
      "excerpt": "Hooks are functions that let you \"hook into\" React state and lifecycle features from function components.",
      "score": 0.95,
      "source": "react"
    }
  ],
  "totalResults": 47,
  "searchTime": 234,
  "creditsUsed": 1
}
```

### Best Practices

<CardGroup cols={2}>
  <Card title="Query Construction">
    - Be specific with framework/library names
    - Include relevant context (e.g., "error handling", "best practices")
    - Use technical terms when appropriate
    - Ask complete questions rather than keywords
  </Card>
  
  <Card title="Result Optimization">
    - Use maxResults to control response size
    - Filter by sources for targeted results
    - Combine multiple searches for comprehensive coverage
    - Follow up with more specific queries if needed
  </Card>
</CardGroup>

## ref_read_url

Extract and read content from any publicly accessible URL.

### Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `url` | string | Yes | URL to read and extract content from |
| `format` | string | No | Output format: `markdown`, `html`, `text` (default: `markdown`) |

### Usage Examples

<AccordionGroup>
  <Accordion title="Read Documentation Page">
    **AI Conversation**:
    ```text
    You: Read the React useState documentation page
    
    Claude: I'll read the React useState documentation for you.
    
    [Uses ref_read_url with URL: "https://react.dev/reference/react/useState"]
    
    According to the useState documentation, useState is a React Hook that lets you add a state variable to your component...
    ```
    
    **Direct Tool Call**:
    ```json
    {
      "name": "ref_read_url",
      "arguments": {
        "url": "https://react.dev/reference/react/useState"
      }
    }
    ```
  </Accordion>

  <Accordion title="Read GitHub README">
    **AI Conversation**:
    ```text
    You: What does the React repository README say?
    
    Claude: Let me read the React repository README file.
    
    [Uses ref_read_url with GitHub URL]
    
    The React README explains that React is a JavaScript library for building user interfaces...
    ```
    
    **Direct Tool Call**:
    ```json
    {
      "name": "ref_read_url",
      "arguments": {
        "url": "https://github.com/facebook/react/blob/main/README.md",
        "format": "markdown"
      }
    }
    ```
  </Accordion>

  <Accordion title="Read Blog Post">
    **AI Conversation**:
    ```text
    You: Summarize this blog post about React performance: https://example.com/react-performance
    
    Claude: I'll read that blog post and summarize it for you.
    
    [Uses ref_read_url with blog URL]
    
    This blog post discusses several React performance optimization techniques...
    ```
    
    **Direct Tool Call**:
    ```json
    {
      "name": "ref_read_url",
      "arguments": {
        "url": "https://example.com/react-performance",
        "format": "text"
      }
    }
    ```
  </Accordion>

  <Accordion title="Read API Documentation">
    **AI Conversation**:
    ```text
    You: Read the Stripe API documentation for creating customers
    
    Claude: I'll read the Stripe customer creation documentation.
    
    [Uses ref_read_url with Stripe docs URL]
    
    According to the Stripe documentation, to create a customer you need to...
    ```
    
    **Direct Tool Call**:
    ```json
    {
      "name": "ref_read_url",
      "arguments": {
        "url": "https://stripe.com/docs/api/customers/create",
        "format": "markdown"
      }
    }
    ```
  </Accordion>
</AccordionGroup>

### Response Format

```json
{
  "url": "https://react.dev/reference/react/useState",
  "title": "useState – React",
  "content": "# useState\n\n`useState` is a React Hook that lets you add a state variable to your component...",
  "metadata": {
    "description": "useState Hook documentation",
    "lastModified": "2024-01-15T10:30:00Z"
  },
  "creditsUsed": 1
}
```

### Supported Content Types

<AccordionGroup>
  <Accordion title="Documentation Sites">
    - Official framework documentation
    - API reference pages
    - Tutorial websites
    - Technical guides
    
    **Examples**:
    - https://react.dev/reference/react/useState
    - https://docs.python.org/3/library/asyncio.html
    - https://nextjs.org/docs/api-reference
  </Accordion>

  <Accordion title="Code Repositories">
    - GitHub README files
    - GitLab documentation
    - Source code files
    - Wiki pages
    
    **Examples**:
    - https://github.com/facebook/react/blob/main/README.md
    - https://raw.githubusercontent.com/microsoft/typescript/main/README.md
    - https://gitlab.com/project/repo/-/blob/main/docs/guide.md
  </Accordion>

  <Accordion title="Blog Posts & Articles">
    - Technical blog posts
    - Tutorial articles
    - News articles
    - Medium posts
    
    **Examples**:
    - https://overreacted.io/a-complete-guide-to-useeffect/
    - https://web.dev/articles/performance-budgets
    - https://css-tricks.com/snippets/css/flexbox/
  </Accordion>

  <Accordion title="Specification Documents">
    - W3C specifications
    - RFC documents
    - Standard definitions
    - Protocol documentation
    
    **Examples**:
    - https://www.w3.org/TR/css-flexbox/
    - https://tools.ietf.org/html/rfc7231
    - https://datatracker.ietf.org/doc/html/rfc6749
  </Accordion>
</AccordionGroup>

## Tool Combinations

AI agents can combine tools for comprehensive assistance:

### Sequential Tool Usage

```text
You: I need to understand how to implement OAuth in Node.js

Claude: I'll help you understand OAuth implementation in Node.js. Let me search for documentation first, then read specific implementation guides.

[Uses ref_search_documentation with query: "OAuth Node.js implementation"]

I found several relevant resources. Let me read the official OAuth documentation for more details.

[Uses ref_read_url to read specific OAuth documentation]

Based on the documentation I found, here's how to implement OAuth in Node.js...
```

### Comparative Research

```text
You: Compare React Context vs Redux for state management

Claude: I'll search for documentation on both approaches and compare them.

[Uses ref_search_documentation with query: "React Context state management"]
[Uses ref_search_documentation with query: "Redux state management React"]

Let me also read the official Redux documentation for their comparison.

[Uses ref_read_url to read Redux documentation]

Based on the documentation from both sources, here's a comparison...
```

### Problem Solving

```text
You: I'm getting a weird error with Next.js routing. Can you help debug this error message: [error details]

Claude: Let me search for information about this specific Next.js routing error.

[Uses ref_search_documentation with query: "Next.js routing error [specific error]"]

I found some relevant information. Let me also read the official Next.js routing documentation to understand the expected behavior.

[Uses ref_read_url with Next.js routing docs URL]

Based on the documentation and error information, this appears to be caused by...
```

## Integration Patterns

### AI Tool Usage Patterns

<AccordionGroup>
  <Accordion title="Proactive Documentation Search">
    AI tools automatically search for relevant documentation when you ask technical questions:
    
    ```text
    You: How do I handle errors in async/await?
    
    [Tool automatically uses ref_search_documentation]
    Claude: Based on JavaScript documentation, here are the best practices for async/await error handling...
    ```
  </Accordion>

  <Accordion title="Contextual URL Reading">
    When you mention or share URLs, AI tools can automatically read them:
    
    ```text
    You: What does this GitHub issue say? https://github.com/facebook/react/issues/12345
    
    [Tool automatically uses ref_read_url]
    Claude: According to the GitHub issue, the problem is related to...
    ```
  </Accordion>

  <Accordion title="Follow-up Research">
    AI tools can use tools for follow-up research based on conversation context:
    
    ```text
    You: I'm building a REST API with Express
    Claude: Great! Let me find the latest Express documentation to help you.
    
    [Uses ref_search_documentation for Express info]
    Based on the Express documentation, here are the key concepts...
    
    You: What about authentication?
    Claude: Let me search for Express authentication patterns.
    
    [Uses ref_search_documentation for Express authentication]
    ```
  </Accordion>
</AccordionGroup>

### Custom Client Integration

For building custom integrations:

```javascript
class RefToolsClient {
  constructor(mcpClient) {
    this.mcp = mcpClient;
  }
  
  async searchDocs(query, options = {}) {
    return await this.mcp.callTool({
      name: 'ref_search_documentation',
      arguments: {
        query,
        maxResults: options.maxResults || 10,
        sources: options.sources
      }
    });
  }
  
  async readUrl(url, format = 'markdown') {
    return await this.mcp.callTool({
      name: 'ref_read_url',
      arguments: { url, format }
    });
  }
  
  async comprehensiveSearch(topic) {
    // Search documentation first
    const docs = await this.searchDocs(topic);
    
    // If we find specific URLs in results, read them for details
    const urlResults = [];
    for (const result of docs.results.slice(0, 3)) {
      const content = await this.readUrl(result.url);
      urlResults.push(content);
    }
    
    return { documentation: docs, details: urlResults };
  }
}
```

## Performance Considerations

### Credit Usage

| Tool | Credit Cost | Notes |
|------|-------------|-------|
| `ref_search_documentation` | 1 credit | Fast, indexed search |
| `ref_read_url` | 1 credit | External URL fetching |

### Response Times

- **Documentation Search**: ~200-500ms (indexed content)
- **URL Reading**: ~1-5 seconds (depends on target site)

### Best Practices

<CardGroup cols={2}>
  <Card title="Efficient Usage">
    - Use documentation search before URL reading
    - Cache results when possible  
    - Combine related queries
    - Use appropriate result limits
  </Card>
  
  <Card title="Error Handling">
    - Handle timeout scenarios gracefully
    - Provide fallback options
    - Validate URLs before reading
    - Monitor credit usage
  </Card>
</CardGroup>

## Troubleshooting

<AccordionGroup>
  <Accordion title="Tools Not Available">
    **Problem**: AI tool doesn't show Ref tools
    
    **Solutions**:
    - Verify MCP server configuration
    - Restart AI tool
    - Check server logs for errors
    - Test server connectivity
  </Accordion>

  <Accordion title="Search Returns No Results">
    **Problem**: ref_search_documentation returns empty results
    
    **Solutions**:
    - Try broader search terms
    - Check if documentation is indexed
    - Verify API key and credits
    - Try different query phrasing
  </Accordion>

  <Accordion title="URL Reading Fails">
    **Problem**: ref_read_url can't access URLs
    
    **Solutions**:
    - Verify URL is publicly accessible
    - Check for authentication requirements
    - Try different URL format
    - Ensure content type is supported
  </Accordion>

  <Accordion title="Slow Performance">
    **Problem**: Tools taking too long to respond
    
    **Solutions**:
    - Check network connectivity
    - Verify target sites are responsive
    - Adjust timeout settings
    - Use smaller result limits
  </Accordion>
</AccordionGroup>

## Debugging Tools

Enable debug mode for detailed logging:

```json
{
  "mcpServers": {
    "ref": {
      "command": "ref-mcp-server",
      "env": {
        "REF_API_KEY": "your-api-key",
        "REF_DEBUG": "true"
      }
    }
  }
}
```

Debug output includes:
- Tool call parameters
- API request/response details
- Timing information
- Error details

## Next Steps

<CardGroup cols={2}>
  <Card
    title="Try in Claude Desktop"
    icon="robot"
    href="/installation/claude"
  >
    Set up Ref tools in Claude Desktop
  </Card>
  <Card
    title="Cursor Integration"
    icon="cursor-click"
    href="/installation/cursor"
  >
    One-click setup in Cursor
  </Card>
  <Card
    title="Build Custom Client"
    icon="code"
    href="/api-reference/introduction"
  >
    Create custom MCP integrations
  </Card>
  <Card
    title="Get Support"
    icon="life-ring"
    href="/support/contact"
  >
    Get help with MCP tools
  </Card>
</CardGroup>

Ready to use Ref tools? Start a conversation with your AI assistant and ask about any technical topic!