---
title: 'Direct API Integration'
description: 'Integrate Ref using REST API calls for custom applications and tools'
---

## Overview

The Ref REST API provides direct access to all documentation search and retrieval features. Perfect for custom applications, web apps, mobile apps, and any tool that can make HTTP requests.

<CardGroup cols={2}>
  <Card
    title="REST API"
    icon="server"
  >
    Simple HTTP endpoints with JSON responses
  </Card>
  <Card
    title="Any Language"
    icon="code"
  >
    Works with Python, JavaScript, Go, Rust, and more
  </Card>
  <Card
    title="Full Control"
    icon="sliders"
  >
    Custom error handling, caching, and rate limiting
  </Card>
  <Card
    title="Scalable"
    icon="chart-line"
  >
    Built for high-volume applications
  </Card>
</CardGroup>

## Quick Start

### 1. Get Your API Key

Copy your API key from the [Ref dashboard](https://ref.tools/dashboard).

### 2. Make Your First Request

<CodeGroup>

```bash cURL
curl -X POST https://ref.tools/api/search \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "React hooks tutorial"
  }'
```

```python Python
import requests

headers = {
    "Authorization": "Bearer YOUR_API_KEY",
    "Content-Type": "application/json"
}

response = requests.post(
    "https://ref.tools/api/search",
    headers=headers,
    json={"query": "React hooks tutorial"}
)

data = response.json()
print(data["results"])
```

```javascript Node.js
const axios = require('axios');

const response = await axios.post('https://ref.tools/api/search', {
  query: 'React hooks tutorial'
}, {
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  }
});

console.log(response.data.results);
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "net/http"
)

func main() {
    payload := map[string]string{"query": "React hooks tutorial"}
    jsonPayload, _ := json.Marshal(payload)
    
    req, _ := http.NewRequest("POST", "https://ref.tools/api/search", 
        bytes.NewBuffer(jsonPayload))
    req.Header.Set("Authorization", "Bearer YOUR_API_KEY")
    req.Header.Set("Content-Type", "application/json")
    
    client := &http.Client{}
    resp, _ := client.Do(req)
    
    // Process response
}
```

</CodeGroup>

## API Endpoints

### Documentation Search

Search through indexed documentation:

<CodeGroup>

```bash Endpoint
POST https://ref.tools/api/search
```

```json Request Body
{
  "query": "React component lifecycle",
  "maxResults": 10,
  "sources": ["react", "typescript"],
  "includeContent": true
}
```

```json Response
{
  "results": [
    {
      "title": "React Component Lifecycle",
      "url": "https://react.dev/reference/react/Component#lifecycle",
      "excerpt": "Every component goes through several phases...",
      "score": 0.92,
      "source": "react",
      "content": "Full content if includeContent=true"
    }
  ],
  "totalResults": 47,
  "searchTime": 234,
  "creditsUsed": 1
}
```

</CodeGroup>

### URL Reading

Extract content from any URL:

<CodeGroup>

```bash Endpoint
POST https://ref.tools/api/read-url
```

```json Request Body
{
  "url": "https://react.dev/reference/react/useState",
  "format": "markdown",
  "extractLinks": true
}
```

```json Response
{
  "url": "https://react.dev/reference/react/useState",
  "title": "useState – React",
  "content": "# useState\n\n`useState` is a React Hook...",
  "metadata": {
    "description": "useState Hook documentation",
    "keywords": ["react", "hooks", "state"],
    "lastModified": "2024-01-15T10:30:00Z"
  },
  "links": ["https://react.dev/learn/state-a-components-memory"],
  "creditsUsed": 1
}
```

</CodeGroup>

### Web Search

Search the web for real-time information:

<CodeGroup>

```bash Endpoint
POST https://ref.tools/api/web-search
```

```json Request Body
{
  "query": "Next.js 15 new features 2024",
  "maxResults": 5,
  "freshness": "week"
}
```

```json Response
{
  "results": [
    {
      "title": "Next.js 15 - What's New",
      "url": "https://nextjs.org/blog/next-15",
      "snippet": "Next.js 15 introduces several new features...",
      "publishedDate": "2024-01-10T00:00:00Z",
      "source": "nextjs.org"
    }
  ],
  "searchTime": 456,
  "creditsUsed": 2
}
```

</CodeGroup>

## Authentication

All API requests require authentication using Bearer tokens:

```bash
Authorization: Bearer ref_api_1234567890abcdef
```

<Warning>
Keep your API key secure:
- Store in environment variables
- Never commit to version control
- Rotate keys regularly
- Use different keys for different environments
</Warning>

## Rate Limiting

The API implements rate limiting to ensure fair usage:

| Plan | Rate Limit | Burst Limit |
|------|------------|-------------|
| **Free** | 100 requests/hour | 10 requests/minute |
| **Basic** | 1,000 requests/hour | 50 requests/minute |
| **Pro** | 5,000 requests/hour | 100 requests/minute |
| **Team** | 10,000 requests/hour | 200 requests/minute |

Rate limit headers are included in responses:

```
X-RateLimit-Limit: 1000
X-RateLimit-Remaining: 999
X-RateLimit-Reset: 1640995200
```

## Error Handling

The API returns standard HTTP status codes:

<AccordionGroup>
  <Accordion title="200 OK">
    Request successful, results returned
    ```json
    {
      "results": [...],
      "creditsUsed": 1
    }
    ```
  </Accordion>

  <Accordion title="400 Bad Request">
    Invalid request format or parameters
    ```json
    {
      "error": "invalid_request",
      "message": "Query parameter is required",
      "code": "MISSING_QUERY"
    }
    ```
  </Accordion>

  <Accordion title="401 Unauthorized">
    Invalid or missing API key
    ```json
    {
      "error": "unauthorized",
      "message": "Invalid API key",
      "code": "INVALID_API_KEY"
    }
    ```
  </Accordion>

  <Accordion title="429 Too Many Requests">
    Rate limit exceeded
    ```json
    {
      "error": "rate_limit_exceeded",
      "message": "Too many requests",
      "retryAfter": 60
    }
    ```
  </Accordion>

  <Accordion title="402 Payment Required">
    Insufficient credits
    ```json
    {
      "error": "insufficient_credits",
      "message": "Not enough credits for this operation",
      "creditsRequired": 2,
      "creditsAvailable": 0
    }
    ```
  </Accordion>
</AccordionGroup>

## SDKs and Libraries

Official SDKs for popular languages:

<CardGroup cols={2}>
  <Card title="JavaScript/TypeScript">
    ```bash
    npm install @ref-tools/js-sdk
    ```
  </Card>
  
  <Card title="Python">
    ```bash
    pip install ref-tools
    ```
  </Card>
  
  <Card title="Go">
    ```bash
    go get github.com/ref-tools/go-sdk
    ```
  </Card>
  
  <Card title="Rust">
    ```bash
    cargo add ref-tools
    ```
  </Card>
</CardGroup>

### SDK Examples

<CodeGroup>

```javascript JavaScript SDK
import { RefClient } from '@ref-tools/js-sdk';

const client = new RefClient({
  apiKey: process.env.REF_API_KEY
});

// Search documentation
const results = await client.search('React hooks');

// Read URL
const content = await client.readUrl('https://react.dev/reference/react/useState');

// Web search
const webResults = await client.webSearch('Next.js 15 features');
```

```python Python SDK
from ref_tools import RefClient

client = RefClient(api_key=os.environ['REF_API_KEY'])

# Search documentation
results = client.search('React hooks')

# Read URL
content = client.read_url('https://react.dev/reference/react/useState')

# Web search
web_results = client.web_search('Next.js 15 features')
```

```go Go SDK
package main

import (
    "github.com/ref-tools/go-sdk"
)

func main() {
    client := ref.NewClient(os.Getenv("REF_API_KEY"))
    
    // Search documentation
    results, err := client.Search("React hooks")
    
    // Read URL
    content, err := client.ReadURL("https://react.dev/reference/react/useState")
    
    // Web search
    webResults, err := client.WebSearch("Next.js 15 features")
}
```

</CodeGroup>

## Advanced Features

### Batch Requests

Process multiple queries efficiently:

```json
POST /api/batch
{
  "requests": [
    {
      "method": "search",
      "params": {"query": "React hooks"}
    },
    {
      "method": "readUrl", 
      "params": {"url": "https://react.dev/reference/react/useState"}
    }
  ]
}
```

### Streaming Responses

For real-time applications, use streaming:

```javascript
const response = await fetch('/api/search-stream', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Accept': 'text/event-stream'
  },
  body: JSON.stringify({query: 'React hooks'})
});

const reader = response.body.getReader();
// Process streaming results
```

### Webhooks

Get notified when indexing jobs complete:

```json
POST /api/webhooks
{
  "url": "https://yourapp.com/webhook",
  "events": ["index.completed", "index.failed"],
  "secret": "webhook-secret-key"
}
```

## Integration Examples

### Web Application

Build a documentation search interface:

```html
<!DOCTYPE html>
<html>
<head>
    <title>Doc Search</title>
</head>
<body>
    <input id="query" placeholder="Search documentation..." />
    <button onclick="search()">Search</button>
    <div id="results"></div>

    <script>
    async function search() {
        const query = document.getElementById('query').value;
        const response = await fetch('/api/search', {
            method: 'POST',
            headers: {
                'Authorization': 'Bearer YOUR_API_KEY',
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({query})
        });
        
        const data = await response.json();
        displayResults(data.results);
    }
    
    function displayResults(results) {
        const container = document.getElementById('results');
        container.innerHTML = results.map(r => 
            `<div><h3><a href="${r.url}">${r.title}</a></h3><p>${r.excerpt}</p></div>`
        ).join('');
    }
    </script>
</body>
</html>
```

### CLI Tool

Create a command-line documentation search tool:

```python
#!/usr/bin/env python3
import os
import sys
import requests

def search_docs(query):
    response = requests.post(
        'https://ref.tools/api/search',
        headers={
            'Authorization': f'Bearer {os.environ["REF_API_KEY"]}',
            'Content-Type': 'application/json'
        },
        json={'query': query}
    )
    
    if response.status_code == 200:
        results = response.json()['results']
        for result in results:
            print(f"{result['title']}")
            print(f"{result['url']}")
            print(f"{result['excerpt']}\n")
    else:
        print(f"Error: {response.status_code}")

if __name__ == '__main__':
    if len(sys.argv) > 1:
        search_docs(' '.join(sys.argv[1:]))
    else:
        print("Usage: search-docs <query>")
```

### Mobile App Integration

Integrate with React Native:

```javascript
import React, { useState } from 'react';
import { View, TextInput, Button, ScrollView, Text } from 'react-native';

const DocSearch = () => {
  const [query, setQuery] = useState('');
  const [results, setResults] = useState([]);

  const search = async () => {
    const response = await fetch('https://ref.tools/api/search', {
      method: 'POST',
      headers: {
        'Authorization': 'Bearer YOUR_API_KEY',
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({query})
    });
    
    const data = await response.json();
    setResults(data.results);
  };

  return (
    <View>
      <TextInput 
        value={query}
        onChangeText={setQuery}
        placeholder="Search documentation..."
      />
      <Button title="Search" onPress={search} />
      <ScrollView>
        {results.map(result => (
          <View key={result.url}>
            <Text>{result.title}</Text>
            <Text>{result.excerpt}</Text>
          </View>
        ))}
      </ScrollView>
    </View>
  );
};
```

## Security Best Practices

<CardGroup cols={2}>
  <Card title="API Key Management">
    - Store keys in environment variables
    - Use different keys per environment
    - Rotate keys regularly
    - Never log API keys
  </Card>
  
  <Card title="Request Security">
    - Always use HTTPS
    - Validate user input
    - Implement request signing for sensitive apps
    - Use IP allowlisting for server apps
  </Card>
  
  <Card title="Error Handling">
    - Don't expose internal errors
    - Log errors for debugging
    - Provide helpful user messages
    - Implement exponential backoff
  </Card>
  
  <Card title="Performance">
    - Implement client-side caching
    - Use batch requests when possible
    - Monitor and optimize query patterns
    - Set appropriate timeouts
  </Card>
</CardGroup>

## Monitoring and Analytics

Track your API usage:

```javascript
// Add usage tracking
const trackUsage = (endpoint, query, responseTime, creditsUsed) => {
  analytics.track('ref_api_usage', {
    endpoint,
    query: query.substring(0, 100), // Truncate for privacy
    responseTime,
    creditsUsed,
    timestamp: Date.now()
  });
};

// Use in your API calls
const startTime = Date.now();
const result = await client.search(query);
const responseTime = Date.now() - startTime;

trackUsage('search', query, responseTime, result.creditsUsed);
```

## Testing

Test your integration thoroughly:

<CodeGroup>

```javascript Jest Tests
describe('Ref API Integration', () => {
  test('should search documentation', async () => {
    const client = new RefClient({apiKey: 'test-key'});
    const results = await client.search('React hooks');
    
    expect(results).toHaveLength(10);
    expect(results[0]).toHaveProperty('title');
    expect(results[0]).toHaveProperty('url');
  });

  test('should handle errors gracefully', async () => {
    const client = new RefClient({apiKey: 'invalid-key'});
    
    await expect(client.search('test'))
      .rejects.toThrow('Unauthorized');
  });
});
```

```python Python Tests
import unittest
from ref_tools import RefClient

class TestRefClient(unittest.TestCase):
    def setUp(self):
        self.client = RefClient(api_key='test-key')
    
    def test_search(self):
        results = self.client.search('React hooks')
        self.assertEqual(len(results), 10)
        self.assertIn('title', results[0])
    
    def test_invalid_key(self):
        client = RefClient(api_key='invalid')
        with self.assertRaises(Exception):
            client.search('test')
```

</CodeGroup>

## Getting Help

<CardGroup cols={2}>
  <Card
    title="API Documentation"
    icon="book"
    href="/api-reference/introduction"
  >
    Complete API reference and examples
  </Card>
  
  <Card
    title="SDK Documentation"
    icon="code"
    href="https://github.com/ref-tools"
  >
    SDK source code and documentation
  </Card>
  
  <Card
    title="Support Forum"
    icon="comments"
    href="https://ref.tools/discord"
  >
    Get help from the community
  </Card>
  
  <Card
    title="Contact Support"
    icon="envelope"
    href="/support/contact"
  >
    Direct support from our team
  </Card>
</CardGroup>

## Next Steps

<CardGroup cols={2}>
  <Card
    title="API Reference"
    icon="book"
    href="/api-reference/introduction"
  >
    Explore detailed API documentation
  </Card>
  <Card
    title="Features Guide"
    icon="star"
    href="/features/search"
  >
    Learn about advanced features
  </Card>
  <Card
    title="Indexing Guide"
    icon="database"
    href="/features/indexing"
  >
    Add your own documentation
  </Card>
  <Card
    title="Team Setup"
    icon="users"
    href="/features/teams"
  >
    Configure team collaboration
  </Card>
</CardGroup>

The Ref API provides powerful, flexible access to documentation search and retrieval. Start building your integration today!