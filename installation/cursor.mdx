---
title: 'Cursor Installation'
description: 'Install Ref in Cursor with one-click deeplink setup'
---

## Overview

Cursor provides seamless integration with Ref through deeplinks and MCP support, making it one of the easiest ways to get documentation search directly in your IDE.

<CardGroup cols={2}>
  <Card
    title="One-Click Install"
    icon="mouse-pointer"
  >
    Use deeplinks for instant Ref installation in Cursor
  </Card>
  <Card
    title="Native Integration"
    icon="puzzle-piece"
  >
    Built-in MCP support for seamless documentation access
  </Card>
</CardGroup>

## Quick Installation (Recommended)

### Method 1: Deeplink Installation

The fastest way to get Ref working in Cursor:

<Steps>
  <Step title="Get Your API Key">
    Copy your API key from the [Ref dashboard](https://ref.tools/dashboard)
  </Step>

  <Step title="Click to Install">
    Click this button to install Ref in Cursor:
    
    <Card title="Install Ref in Cursor" href="cursor://install-mcp?name=ref&package=@ref-tools/mcp-server&env=REF_API_KEY">
      🚀 One-Click Install in Cursor
    </Card>
    
    *This will open Cursor and automatically configure Ref for you.*
  </Step>

  <Step title="Enter API Key">
    When prompted, paste your API key and confirm the installation
  </Step>

  <Step title="Start Using">
    Ref is now available in Cursor! Try asking about documentation in your conversations.
  </Step>
</Steps>

<Tip>
Make sure Cursor is installed and running before clicking the install button.
</Tip>

### Method 2: Generate Custom Deeplink

For team installations or custom configurations:

<CodeGroup>

```javascript Generate Deeplink
// Visit ref.tools/cursor to generate a custom deeplink
// with your API key pre-filled
```

```bash Manual URL Construction
cursor://install-mcp?name=ref&package=@ref-tools/mcp-server&env=REF_API_KEY=your-api-key-here
```

</CodeGroup>

## Manual Installation

If deeplinks don't work, you can install manually:

<Steps>
  <Step title="Open Cursor Settings">
    Navigate to Cursor Settings → Extensions → MCP Servers
  </Step>

  <Step title="Add New MCP Server">
    Click "Add Server" and enter:
    - **Name:** `ref`
    - **Package:** `@ref-tools/mcp-server`
    - **Command:** `ref-mcp-server`
  </Step>

  <Step title="Configure Environment">
    Add environment variable:
    - **Key:** `REF_API_KEY`
    - **Value:** `your-api-key-here`
  </Step>

  <Step title="Save and Restart">
    Save the configuration and restart Cursor
  </Step>
</Steps>

## Cursor Configuration File

Alternatively, edit Cursor's MCP configuration directly:

<Steps>
  <Step title="Open Config File">
    Open Cursor's MCP configuration file:
    
    **Location varies by OS, typically:**
    - macOS: `~/Library/Application Support/Cursor/mcp_config.json`
    - Windows: `%APPDATA%\Cursor\mcp_config.json`
    - Linux: `~/.config/Cursor/mcp_config.json`
  </Step>

  <Step title="Add Ref Configuration">
    ```json
    {
      "mcpServers": {
        "ref": {
          "command": "npx",
          "args": ["@ref-tools/mcp-server"],
          "env": {
            "REF_API_KEY": "your-api-key-here"
          }
        }
      }
    }
    ```
  </Step>

  <Step title="Restart Cursor">
    Close and restart Cursor to load the new configuration
  </Step>
</Steps>

## Using Ref in Cursor

Once installed, Ref integrates seamlessly with Cursor's AI features:

### Chat Integration

In Cursor's chat panel, you can:

<AccordionGroup>
  <Accordion title="Ask Documentation Questions">
    **Example:** "How do I use React hooks?"
    
    Cursor will automatically search relevant documentation and provide comprehensive answers.
  </Accordion>

  <Accordion title="Request Specific URLs">
    **Example:** "Read the Next.js API routes documentation"
    
    Cursor will fetch and analyze the specific documentation page.
  </Accordion>

  <Accordion title="Get Contextual Help">
    **Example:** "Explain this error with documentation"
    
    Cursor will search for error-specific documentation and solutions.
  </Accordion>
</AccordionGroup>

### Code Editor Integration

Ref also works with Cursor's inline features:

- **Ctrl/Cmd + K**: Ask questions about code with documentation context
- **Code explanations**: Get documentation-backed explanations of code blocks
- **Error resolution**: Find solutions using indexed documentation

### Example Workflows

<CodeGroup>

```text Documentation Search
You: How do I handle async/await errors in JavaScript?

Cursor: [Uses ref_search_documentation]
I'll search for async/await error handling documentation...

Based on the documentation, here are the best practices for handling async/await errors...
```

```text URL Reading  
You: Read the React useEffect documentation

Cursor: [Uses ref_read_url]
I'll read the React useEffect documentation for you...

Here's the key information from the useEffect documentation...
```

```text Contextual Help
You: @ref explain this React component lifecycle

Cursor: [Uses ref_search_documentation with context]
Let me search for React component lifecycle information...

Based on the documentation and your code context...
```

</CodeGroup>

## Advanced Configuration

### Team Setup

For teams using Ref, configure team-specific access:

```json
{
  "mcpServers": {
    "ref": {
      "command": "npx",
      "args": ["@ref-tools/mcp-server"],
      "env": {
        "REF_API_KEY": "team-api-key-here",
        "REF_TEAM_ID": "your-team-id"
      }
    }
  }
}
```

### Custom Configuration

Customize Ref's behavior in Cursor:

```json
{
  "mcpServers": {
    "ref": {
      "command": "npx",
      "args": ["@ref-tools/mcp-server"],
      "env": {
        "REF_API_KEY": "your-api-key",
        "REF_MAX_RESULTS": "15",
        "REF_TIMEOUT": "45000",
        "REF_CACHE_ENABLED": "true"
      }
    }
  }
}
```

### Multiple Configurations

Run multiple Ref configurations for different projects:

```json
{
  "mcpServers": {
    "ref-personal": {
      "command": "npx",
      "args": ["@ref-tools/mcp-server"],
      "env": {
        "REF_API_KEY": "personal-api-key"
      }
    },
    "ref-work": {
      "command": "npx",
      "args": ["@ref-tools/mcp-server"],
      "env": {
        "REF_API_KEY": "work-api-key",
        "REF_TEAM_ID": "work-team-id"
      }
    }
  }
}
```

## Best Practices

### Optimizing Performance

<CardGroup cols={2}>
  <Card title="Specific Queries">
    Ask specific questions rather than broad ones for faster, more relevant results
  </Card>
  
  <Card title="Use Context">
    Reference specific technologies or frameworks in your questions
  </Card>
  
  <Card title="Cache Results">
    Enable caching to speed up repeated queries
  </Card>
  
  <Card title="Monitor Credits">
    Keep track of your credit usage in the Ref dashboard
  </Card>
</CardGroup>

### Effective Usage Patterns

```text
✅ Good: "How do I implement pagination in React with hooks?"
❌ Vague: "Help with React"

✅ Good: "Show me Next.js API middleware examples"  
❌ Too broad: "Next.js help"

✅ Good: "Read the TypeScript decorators documentation"
❌ Unclear: "TypeScript stuff"
```

## Troubleshooting

<AccordionGroup>
  <Accordion title="Deeplink Not Working">
    **Symptoms:** Clicking install button does nothing
    
    **Solutions:**
    - Ensure Cursor is installed and running
    - Try copying the deeplink URL and opening it manually
    - Check if your browser blocks custom URL schemes
    - Use manual installation method instead
  </Accordion>

  <Accordion title="MCP Server Not Found">
    **Symptoms:** "ref-mcp-server command not found" error
    
    **Solutions:**
    - Install globally: `npm install -g @ref-tools/mcp-server`
    - Use npx in command: `npx @ref-tools/mcp-server`
    - Check Node.js is installed: `node --version`
    - Verify npm is in PATH
  </Accordion>

  <Accordion title="API Key Issues">
    **Symptoms:** Authentication errors in Cursor
    
    **Solutions:**
    - Double-check API key is copied correctly
    - Verify no extra spaces or characters
    - Generate a new API key if needed
    - Check API key hasn't expired
  </Accordion>

  <Accordion title="No Search Results">
    **Symptoms:** Cursor can't find documentation
    
    **Solutions:**
    - Check credit balance in dashboard
    - Try more specific search terms
    - Verify internet connection
    - Check if documentation is indexed in Ref
  </Accordion>
</AccordionGroup>

### Debug Mode

Enable debug mode to troubleshoot issues:

```json
{
  "mcpServers": {
    "ref": {
      "command": "npx",
      "args": ["@ref-tools/mcp-server"],
      "env": {
        "REF_API_KEY": "your-api-key",
        "DEBUG": "ref:*"
      }
    }
  }
}
```

## Updates and Maintenance

### Automatic Updates

Cursor will typically update MCP servers automatically, but you can force updates:

```bash
npm update -g @ref-tools/mcp-server
```

### Version Management

Check your current version:

```bash
npm list -g @ref-tools/mcp-server
```

### Configuration Backup

Save your configuration for team sharing:

```bash
# Export configuration
cp ~/.config/Cursor/mcp_config.json ref-cursor-config.json

# Share with team or backup
git add ref-cursor-config.json
```

## Team Deployment

### Organization Setup

For teams deploying Ref across multiple Cursor installations:

<Steps>
  <Step title="Create Team Account">
    Set up a team account on [ref.tools](https://ref.tools/teams)
  </Step>

  <Step title="Generate Team API Key">
    Create a shared API key for team use
  </Step>

  <Step title="Create Installation Package">
    ```bash
    # Create install script
    echo 'cursor://install-mcp?name=ref&package=@ref-tools/mcp-server&env=REF_API_KEY=team-key&env=REF_TEAM_ID=team-id' > install-ref.txt
    ```
  </Step>

  <Step title="Distribute to Team">
    Share the installation link or script with team members
  </Step>
</Steps>

## Getting Help

<CardGroup cols={2}>
  <Card
    title="Cursor Community"
    icon="users"
    href="https://cursor.directory"
  >
    Get help from the Cursor community
  </Card>
  
  <Card
    title="Ref Support"
    icon="life-ring"
    href="/support/contact"
  >
    Contact Ref support for integration issues
  </Card>
</CardGroup>

## Next Steps

Now that Ref is installed in Cursor:

<CardGroup cols={2}>
  <Card
    title="Explore Documentation"
    icon="book-open"
    href="/features/search"
  >
    Learn advanced search techniques
  </Card>
  <Card
    title="Index Custom Docs"
    icon="database"
    href="/features/indexing"
  >
    Add your project documentation
  </Card>
  <Card
    title="Team Features"
    icon="users"
    href="/features/teams"
  >
    Set up team collaboration
  </Card>
  <Card
    title="API Reference"
    icon="code"
    href="/api-reference/introduction"
  >
    Explore advanced integrations
  </Card>
</CardGroup>

Ref is now integrated with Cursor! Start asking documentation questions in your conversations and see how it enhances your development workflow.